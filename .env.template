# ==============================
# Mem0 Deployment Lab (.env)
# Copy this file to .env and fill in values.
# ==============================

# --- Provider Selection ---
# Default track uses OpenAI.
# Optional AWS-only track uses Bedrock for BOTH LLM + embeddings.
LLM_PROVIDER=openai            # openai | aws_bedrock
EMBEDDER_PROVIDER=openai       # openai | aws_bedrock

# --- OpenAI Configuration (ONLY if using OpenAI provider) ---
OPENAI_API_KEY=your_openai_key_here
# Optional: custom OpenAI-compatible endpoint (rare)
# OPENAI_BASE_URL=https://api.openai.com/v1

# --- AWS Bedrock Configuration (ONLY if using aws_bedrock provider) ---
# Preferred on EC2: attach an IAM Role to the instance with Bedrock permissions (no keys needed).
AWS_REGION=us-east-1
# Optional (if NOT using an IAM role):
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_SESSION_TOKEN=your_aws_session_token
# AWS_PROFILE=default

# --- Qdrant Configuration ---
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# --- API Configuration ---
API_HOST=0.0.0.0
API_PORT=8000
API_KEY=your_generated_api_key_here

# --- LLM Configuration ---
# OpenAI example:
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.7

# Bedrock example (if LLM_PROVIDER=aws_bedrock):
# LLM_MODEL=anthropic.claude-3-5-sonnet-20240620-v1:0
# LLM_TEMPERATURE=0.7
# LLM_MAX_TOKENS=2000
# LLM_TOP_P=0.9
# LLM_TOP_K=1

# --- Embeddings Configuration ---
# OpenAI example:
EMBEDDER_MODEL=text-embedding-3-small

# Bedrock Titan example (if EMBEDDER_PROVIDER=aws_bedrock):
# EMBEDDER_MODEL=amazon.titan-embed-text-v1

# --- Admin API Key (optional - defaults to same as API_KEY) ---
# ADMIN_API_KEY=your_admin_key_here
